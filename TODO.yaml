- FIX:
    - get the dependency tree of the snippets

- FEAT:
    - support parametrized constructs
    - virtual classes
    - static functions
    - class inheritance

- CHORE:
    - make unit tests for the modules
    - Add a real CI to check that builds are clean
    - Add precommit hooks
